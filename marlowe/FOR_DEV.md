uv run uvicorn api:app --reload

rather than streaming only prose, the backend must communicate safe, structured investigation events such as “opened file,” “completed review,” and a cited finding with its source file and locator. You would validate those events directly from the API before I connect them to activity cards, evidence citations, and the case board.
That keeps the UI from dictating your agent logic, while ensuring the two parts integrate cleanly.